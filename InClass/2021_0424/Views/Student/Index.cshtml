
@using Apr24_.Models
@model IEnumerable<StudentCourseViewModel>

<h2>Index</h2>


<div class="container">

    @foreach (var m in Model)
    {
        <div class="card">

            <h4>@m.student.stuName </h4>
            <hr />
            <label>@m.student.stuID</label>
            @foreach (var c in m.coursesTaken) { 
            
                <h5> @c.courseTitle</h5>
                <label> @c.courseID </label>
                <hr />
            
            }


        </div>
    }

</div>

